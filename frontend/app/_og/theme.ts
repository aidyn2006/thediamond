/**
 * OG-image palette. Satori resolves no CSS variables, so the share cards need the
 * literal values — keep these in sync with the tokens in `app/globals.css`.
 */
export const OG = {
  bg: "#f4fbf8",
  surface: "#ffffff",
  surface2: "#e9f7f1",
  border: "#ceede2",
  text: "#0d2b23",
  dim: "#55736a",
  prism: "linear-gradient(135deg, #74f2c4 0%, #a3f0dc 55%, #ceede2 100%)",
} as const;
