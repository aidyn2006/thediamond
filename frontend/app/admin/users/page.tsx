import { StagePlaceholder } from "@/components/app/StagePlaceholder";

export default function AdminUsersPage() {
  return (
    <StagePlaceholder
      title="Пользователи"
      note="Поиск пользователей и блокировка (бан). Раздел собираем на этапе 8."
    />
  );
}
